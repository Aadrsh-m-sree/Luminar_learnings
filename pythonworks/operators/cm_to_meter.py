# create a python programme to convert from centimetre to metre

def cm_to_meter():
# meter = centimeters // 100
    centimeters = float(input("Enter Eentimeters :"))
    meter = centimeters // 100
    print(f"{centimeters} centimeters are {meter} meters")
		
cm_to_meter()

